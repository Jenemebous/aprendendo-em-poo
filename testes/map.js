// elevado

/*
const numeros = [1, 2, 4, 8];

const elevado = numeros.map((numero) => {
    return numero ** 2

});

console.log(elevado);
*/

// maiúsculo

/*

const nomes = ["Vanessa", "Felipe", "Rosana"];

const mm = nomes.map((nome) => {
    return nome.toUpperCase()
})

console.log(mm)

*/

// minusculo

/*
const nomes = ["VANESSA", "FELIPE", "ROSANA"];

const mm = nomes.map((nome) => {
    return nome.toLowerCase()
})

console.log(mm)
*/

// pares

/*
const numeros = [1, 3, 4, 8];

const pares = numeros.map((numero) => {
 if(numero % 2 === 0){
    return "esses são os números pares: " + numero
 }
})

console.log(pares)
*/